# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.27.1'

    - name: Build
      run: go build -v ./...

  test:
    - name: Test
      run: ./test.sh

    - name: Archive test results
      uses: actions/upload-artifact@v4
      with:
        name: test-report
        path: test-report

    - name: Add Summary
      run: |
        cat test-report/test_report.md >> $GITHUB_STEP_SUMMARY
        cat test-report/coverage.md >> $GITHUB_STEP_SUMMARY
